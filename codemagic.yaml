workflows:
  rapidfire-android-apk:
    name: RapidFire Panel — Android APK
    max_build_duration: 30
    environment:
      node: 20.17.0
      java: 17

    scripts:
      # ── Step 1: Install only what Gradle needs (no CLI) ────────────
      - name: Install Capacitor Android dependency
        script: |
          cd $CM_BUILD_DIR/..
          npm install --ignore-scripts \
            @capacitor/android \
            @capacitor/core

      # ── Step 2: Build APK ──────────────────────────────────────────
      - name: Build Android APK
        script: |
          cd $CM_BUILD_DIR
          chmod +x ./gradlew
          ./gradlew assembleDebug --no-daemon

    artifacts:
      - app/build/outputs/apk/debug/app-debug.apk
      - app/build/outputs/apk/**/*.apk

    publishing:
      email:
        recipients:
          - your@email.com
        notify:
          success: true
          failure: true
